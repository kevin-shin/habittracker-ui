import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    addHabitButton: {
        marginBottom: 20,
        alignSelf: "center"
    },
    addHabitButtonText: {
        fontSize: 20,
        color: "blue"
    }
});
